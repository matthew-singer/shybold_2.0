#ifndef BIO_H_
#define BIO_H_

#include <vector>
#include <memory>


#include "params.h"


class chrome {
public:
	chrome() {
		for (vector<double>iterator dbl_it = values.begin(); dbl_it != values.end(); ++dbl_it) {
			if (mutate) {
				*it += m
			}	
		}

	}	

	std::vector<double> values(geneCount);


}

class genome {
public:
	std::unique_ptr<chrome> c1;
	std::unique_ptr<chrome> c1;
	
	class genome(c *c1, chrome *c2) {
		
	}

}



#endif
